//
// Created by Jose on 13/09/2024.
//
#include <gtest/gtest.h>
#include "QuickSort.h"
#include "DoubleLinkedList.h"
/*
// Asegúrate de que la lista esté ordenada de forma ascendente después de ordenarla con QuickSort
TEST(QuickSortTest, SortsAscendingOrder) {
    DoubleLinkedList list{5, 1, 4, 2, 8};
    QuickSort::sort(list);

    EXPECT_EQ(list.get(0), 1);
    EXPECT_EQ(list.get(1), 2);
    EXPECT_EQ(list.get(2), 4);
    EXPECT_EQ(list.get(3), 5);
    EXPECT_EQ(list.get(4), 8);
}

// Verifica que la lista esté ordenada de forma ascendente incluso si ya está ordenada
TEST(QuickSortTest, SortsAlreadySortedList) {
    DoubleLinkedList list{1, 2, 3, 4, 5};
    QuickSort::sort(list);

    EXPECT_EQ(list.get(0), 1);
    EXPECT_EQ(list.get(1), 2);
    EXPECT_EQ(list.get(2), 3);
    EXPECT_EQ(list.get(3), 4);
    EXPECT_EQ(list.get(4), 5);
}

// Verifica que QuickSort maneje correctamente una lista vacía
TEST(QuickSortTest, SortsEmptyList) {
    DoubleLinkedList list{};
    QuickSort::sort(list);

    EXPECT_EQ(list.sizeList(), 0);
}

// Verifica que QuickSort maneje correctamente una lista con un solo elemento
TEST(QuickSortTest, SortsSingleElementList) {
    DoubleLinkedList list{1};
    QuickSort::sort(list);

    EXPECT_EQ(list.get(0), 1);
    EXPECT_EQ(list.sizeList(), 1);
}*/

